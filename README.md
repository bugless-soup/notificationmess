# notificationmess

Just a simple notification server.

Run with:

`$ python3 run.py`

Requirements:

- PyGObject
- Pillow
- numpy
- asyncio-glib
- gtk-layer-shell
- gnu-free-fonts

Test with (on another terminal):

`$ notify-send 'Notification Text'`
